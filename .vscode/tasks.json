{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Client (Next.js)",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}/client"
      },
      "problemMatcher": []
    },
    {
      "label": "Start Server (Express)",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}/server"
      },
      "problemMatcher": []
    },
    {
      "label": "Start Both (Client + Server)",
      "dependsOn": ["Start Client (Next.js)", "Start Server (Express)"],
      "dependsOrder": "parallel",
      "problemMatcher": []
    }
  ]
}
